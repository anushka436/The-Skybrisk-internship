Beginning with variables and fundamental data types like int, float, string, and boolean, I gained an understanding of the foundations of Python programming. I also learned that Python employs dynamic typing. I discovered that the `input()` function always returns a string, necessitating type conversion using `int()` or `float()` when working with numbers, after practicing receiving user input and displaying output. I investigated a variety of operators, such as logical operators (and, or), comparison operators (==,!=, >, <), and arithmetic operators (+, -, *, /, %). Using `if`, `elif`, and `else`, I learned how to create conditional statements that handle special cases like division by zero in a calculator program and include nested conditions.
rrelations in the dataset.